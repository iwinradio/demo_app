<h1>Listing micgroposts</h1>

<table>
  <tr>
    <th>Content</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @micgroposts.each do |micgropost| %>
  <tr>
    <td><%= micgropost.content %></td>
    <td><%= micgropost.user_id %></td>
    <td><%= link_to 'Show', micgropost %></td>
    <td><%= link_to 'Edit', edit_micgropost_path(micgropost) %></td>
    <td><%= link_to 'Destroy', micgropost, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Micgropost', new_micgropost_path %>
